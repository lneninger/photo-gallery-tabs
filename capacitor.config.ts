import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'photo-gallery-tabs',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
